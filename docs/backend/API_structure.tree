api
├── auth                               # Authentication & user identity                                     ✅ tested
│   ├── POST register                  # Create a new user account                                              ✅ tested
│   ├── POST login                     # Authenticate user, returns JWT                                         ✅ tested
│   └── GET me                         # Get current logged-in user (JWT protected)                             ✅ tested
│
├── products                           # Product catalog management                                         ✅ tested
│   ├── GET /                          # List all products (supports ?search=&category=&page=&limit=)           ✅ tested
│   ├── GET {id}                       # Get a product by its ID                                                ✅ tested
│   ├── POST /                         # Create new product (admin only)                                        ✅ tested
│   ├── PUT {id}                       # Update product details (admin only)                                    ✅ tested
│   └── DELETE {id}                    # Delete a product (admin only)                                          ✅ tested
│
├── cart                               # Shopping cart operations                                           ✅ tested
│   ├── GET /                          # Get current user's cart                                                ✅ tested
│   ├── POST items                     # Add an item to cart { product_id, quantity }                           ✅ tested
│   ├── PUT items/{cart_item_id}       # Update quantity of a cart item                                         ✅ tested
│   └── DELETE items/{cart_item_id}    # Remove item from cart                                                  ✅ tested
│
├── checkout                           # Checkout process                                                   ✅ tested
│   └── POST /                         # Validate items, deduct stock, create order                             ✅ tested
│
├── orders                             # Order management                                                   ✖  untested
│   ├── GET /                          # List current user's orders                                             ✖  untested
│   ├── GET {id}                       # Get specific order (must belong to user)                               ✖  untested
│   └── PUT {id}                       # Update order status (business owner/admin only)                        ✖  untested
│
└── admin                              # Administrative endpoints                                           ✖  untested
    ├── GET inventory                  # View all products with stock info                                      ✖  untested
    └── GET reports/sales              # Generate sales report (?start_date=&end_date=)                         ✖  untested